language: csharp
mono: none
dotnet: 5.0.301
solution: /src/Serenity.Net.sln

addons:
  sonarcloud:
    organization: "berkdulger-github"
    token: SECRET_KEY


script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - sonar-scanner -X